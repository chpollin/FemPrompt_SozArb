# Core dependencies for FemPrompt Research Pipeline
# Install with: pip install -r requirements.txt

# Core packages
requests>=2.31.0
pyyaml>=6.0
python-dotenv>=1.0.0  # For environment variable management

# PDF processing
PyPDF2>=3.0.0
beautifulsoup4>=4.12.0

# AI API for summarization
anthropic>=0.68.0  # Claude Haiku 4.5 (summarize-documents.py)

# Excel workflow (required for assessment)
pandas>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# Zotero integration (required for API features)
pyzotero>=1.5.0

# Advanced PDF conversion (INSTALLED - recommended for best quality)
docling>=2.60.0