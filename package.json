{
  "name": "femprompt-prisma-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Headless test harness for the docs/ front end (PRISM pure functions plus Evidence Companion smoke tests). The app stays framework-free and dependency-free; jsdom is a dev dependency of the harness only, never shipped (plan P1).",
  "type": "module",
  "scripts": {
    "test": "node tests/run.mjs && node tests/companion.mjs",
    "test:prism": "node tests/run.mjs",
    "test:companion": "node tests/companion.mjs"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
